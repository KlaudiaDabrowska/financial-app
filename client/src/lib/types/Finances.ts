export enum Finances {
  expense = "expense",
  income = "income",
  saving = "saving",
}

export enum PaymentType {
  cash = "cash",
  card = "card",
}

export enum Currency {
  PLN = "PLN",
  USD = "USD",
  GBP = "GBP",
}
